# 参与贡献

请先阅读 [README](README.md) 与 [架构说明](docs/ARCHITECTURE.md)。项目目前使用直接 Swift 编译，测试和构建不依赖包管理器。

```sh
zsh test.command
zsh build.command
```

解析问题请提供最小、脱敏的事件顺序，以及预期状态和实际状态。优先在 `Sources/Tests.swift` 增加一个能复现问题的合成序列，再修改解析器。不要提交真实账号标识、完整 Power.log、个人套牌或下载卡牌库。

PR 说明应包含：触发问题的场景、修改后的行为、验证方式和仍未覆盖的情况。UI 修改请使用演示数据截图，标明测试的 macOS、屏幕尺寸以及是否开启点击穿透。

新增卡牌机制应说明日志证据如何支持推断；信息不足时显示未知，不猜测对手隐藏卡牌。避免把单份日志成功推广为所有构筑或客户端版本均可用。
